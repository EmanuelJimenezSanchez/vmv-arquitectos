---
import { ClientRouter } from 'astro:transitions'
import Analytics from '@vercel/analytics/astro'

import '@/styles/global.css'

import Header from '../sections/Header.astro'
import Footer from '@/sections/Footer.astro'
import Button from '@/components/ui/Button.astro'

const SITE_URL = 'https://www.vmv-arquitectos.com'
const DEFAULT_OG_IMAGE = `https://cdn.vmv-arquitectos.com/og.jpg`
const DEFAULT_OG_ALT = 'VMV Arquitectos — Diseño y Construcción de Espacios Funcionales y Estéticos'

interface Props {
  title: string
  description?: string
  canonical?: string
  robots?: string
  ogImage?: string
  ogImageAlt?: string
  ogUrl?: string
  ogType?: 'website' | 'article' | 'profile'
  /** Site chrome; disable on full-bleed routes (e.g. combate detail). */
  showHeader?: boolean
}

const {
  title,
  description = 'VMV Arquitectos - Diseño y Construcción de Espacios Funcionales y Estéticos',
  canonical,
  robots,
  ogImage = DEFAULT_OG_IMAGE,
  ogImageAlt = DEFAULT_OG_ALT,
  ogUrl,
  ogType = 'website',
  showHeader = true,
} = Astro.props

const cleanPathname = Astro.url.pathname === '/' ? '/' : Astro.url.pathname.replace(/\/$/, '')
const canonicalURL = canonical ?? `${SITE_URL}${cleanPathname}`
const finalOgUrl = ogUrl ?? canonicalURL
const finalRobots = robots ?? 'index, follow'
---

<!doctype html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>VMV Arquitectos - Diseño y Construcción de Espacios Funcionales y Estéticos</title>

    <link rel="icon" href="/favicon.ico" sizes="48x48" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="apple-touch-icon" href="/apple-touch-icon.png" />
    <link rel="manifest" href="/site.webmanifest" />

    <!--
      Pruebas de tipografía (temporal): se cargan las 4 opciones a la vez
      para poder alternarlas solo cambiando las variables en global.css.
      Una vez elegida la definitiva, quitar este link y dejar solo las
      familias/weights realmente usados.
    -->
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Fraunces:opsz,wght@9..144,200;9..144,300;9..144,400;9..144,500&family=Inter:wght@300;400;500&family=Space+Grotesk:wght@300;400;500&family=Cormorant+Garamond:wght@300;400;500&family=Jost:wght@300;400;500&family=Manrope:wght@200;300;400;500&display=swap"
      rel="stylesheet"
    />

    <meta
      name="description"
      content="VMV Arquitectos - Diseño y Construcción de Espacios Funcionales y Estéticos"
    />
    <meta name="author" content="VMV Arquitectos" />
    <link rel="canonical" href="https://www.vmv-arquitectos.com/" />
    <meta name="robots" content="index, follow" />

    <script is:inline>
      (() => {
        const storageKey = 'vmv-theme'

        const getStoredTheme = () => {
          try {
            const stored = localStorage.getItem(storageKey)
            return stored === 'light' || stored === 'dark' ? stored : null
          } catch {
            return null
          }
        }

        const storedTheme = getStoredTheme()
        const systemTheme = window.matchMedia('(prefers-color-scheme: dark)').matches
          ? 'dark'
          : 'light'
        const resolvedTheme = storedTheme ?? systemTheme

        document.documentElement.classList.toggle('dark', resolvedTheme === 'dark')
        document.documentElement.setAttribute('data-theme', resolvedTheme)
      })()
    </script>

    <slot name="head" />
    <ClientRouter />
    <Analytics />
  </head>
  <body>
    {showHeader && <Header />}
    <main id="main-content">
      <slot />
    </main>
    <Footer />

    {
      showHeader && (
        <div class="fixed right-4 bottom-4 z-60 flex flex-col items-end gap-3 md:right-8 md:bottom-8">
          
        {/* //   <button>
        //     type="button"
        //     id="back-to-top"
        //     aria-label="Volver al inicio"
        //     class="flex h-11 w-11 translate-y-2 items-center justify-center rounded-full border border-vmv-gray-9 bg-vmv-gray-9 text-white opacity-0 shadow-lg transition-[opacity,transform,background-color,border-color] duration-300 pointer-events-none hover:bg-vmv-gray-10 hover:border-vmv-gray-10 data-[visible=true]:pointer-events-auto data-[visible=true]:translate-y-0 data-[visible=true]:opacity-100 cursor-pointer"
        //   >
        //     <svg width="18" height="18" viewBox="0 0 16 16" fill="none" aria-hidden="true">
        //       <path
                // d="M8 12.5V3.5M3.5 8L8 3.5 12.5 8"
        //         stroke="currentColor"
        //         stroke-width="1.4"
        //         stroke-linecap="round"
        //         stroke-linejoin="round"></path>
        //     </svg>
        //   </button> */}
          <Button href="/#contacto" variant="gray" size="lg" class="shadow-lg">
            Contacto
          </Button>
        </div>
      )
    }

    <script type="module">
      import { initLenis } from '../scripts/lenis.js'
      initLenis()
    </script>

    <!-- <script>
      import { $ } from '@/lib/dom-selector'

      const setupBackToTop = () => {
        const button = $<HTMLButtonElement>('#back-to-top')
        if (!button) return

        ;(button as HTMLElement & { __abortController?: AbortController }).__abortController?.abort()
        const controller = new AbortController()
        ;(button as HTMLElement & { __abortController?: AbortController }).__abortController =
          controller
        const { signal } = controller

        const toggleVisibility = () => {
          if (window.scrollY > window.innerHeight * 0.5) {
            button.setAttribute('data-visible', 'true')
          } else {
            button.removeAttribute('data-visible')
          }
        }

        toggleVisibility()
        window.addEventListener('scroll', toggleVisibility, { passive: true, signal })

        button.addEventListener(
          'click',
          () => {
            const lenis = window.lenis as (Window['lenis'] & { scrollTo?: (target: number, options?: { duration?: number }) => void }) | undefined
            if (lenis?.scrollTo) {
              lenis.scrollTo(0, { duration: 1.2 })
            } else {
              window.scrollTo({ top: 0, behavior: 'smooth' })
            }
          },
          { signal },
        )
      }

      document.addEventListener('astro:page-load', setupBackToTop)
      setupBackToTop()
    </script> -->
  </body>
</html>
